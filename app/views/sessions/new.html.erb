
<% if flash[:message] %>
<p><%= flash[:message] %></p>
<% end %>




<%= form_for :user, url: sessions_path, html: {class: "ui middle alinged center aligned"} do |f| %>
    <!-- <h2 class="ui teal image header">
      <%= image_tag 'raccoon.png', height: 50 %>
      <div class="content">
        Log-in to your account
      </div>
    </h2> -->
      <div class="ui stacked segment">
        <div class="field">
          <div class="ui left icon input">
            <i class="user icon"></i>
            <%= f.label :email, "Email" %>
            <%= f.text_field :email %>
          </div>
        </div>
        <div class="field">
          <div class="ui left icon input">
            <i class="lock icon"></i>
            <%= f.label :password, "Password" %>
            <%= f.text_field :password %>
          </div>
        </div>
        <div class="ui fluid large teal submit button">Login</div>
      </div>
    </form>
    <% end %>

    <div class="ui message">
      New to us? <%= link_to "Sign up now!", new_user_path %> Sign Up</a>
    </div>
  </div>

